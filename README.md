# CSFinalProject

## Branch Protection Rules

The `main` branch is protected with the following rules:

- **Require pull request before merging:** All changes must be made through a pull request.
- **Require status checks to pass before merging:** Both the backend and frontend CI workflows must pass successfully.
- **No direct pushes to `main`:** Pushing directly to the `main` branch is not allowed.
