# Test database — auto-created on first `npm test` run (needs PostgreSQL running)
DATABASE_URL=postgresql://fixlt:fixlt@localhost:5432/fixit_test
TEST_DATABASE_URL=postgresql://fixlt:fixlt@localhost:5432/fixit_test

# Firebase — placeholder values (auth middleware is mocked in all tests)
FIREBASE_PROJECT_ID=test-project
FIREBASE_CLIENT_EMAIL=test@test-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\ntest\n-----END RSA PRIVATE KEY-----"
